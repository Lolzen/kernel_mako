--- drivers/hid/hid-picolcd.c
+++ drivers/hid/hid-picolcd.c
@@ -1424,7 +1424,7 @@
 		buf += 10;
 		cnt -= 10;
 	}
-	if (!report)
+	if (!report || report->maxfield != 1)
 		return -EINVAL;
 
 	while (cnt > 0 && (buf[cnt-1] == '\n' || buf[cnt-1] == '\r'))
