--- drivers/usb/gadget/f_midi.c
+++ drivers/usb/gadget/f_midi.c
@@ -416,6 +416,7 @@
 	midi->id = NULL;
 
 	usb_free_descriptors(f->descriptors);
+	usb_free_descriptors(f->hs_descriptors);
 	kfree(midi);
 }
 
